<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, article_path(tag)%>
    </li>
    <% if logged_in? %>
      <p><%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"}%></p>
    <%end%>  
  <% end %>
</ul>
